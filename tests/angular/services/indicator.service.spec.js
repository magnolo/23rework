ngDescribe({
    name: 'Test IndicatorService',
    modules: 'app',
    inject: 'IndicatorService',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
